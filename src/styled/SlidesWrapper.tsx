import styled from 'styled-components'

const SlidesWrapper = styled.div`
    margin: auto;
    position: relative;
    display: flex;
    overflow: hidden;
    user-select: none;
`

export default SlidesWrapper
